# دليل بناء iOS - سريع

## ✅ حالة الإعدادات

جميع ملفات iOS جاهزة ومعدة للبناء:
- ✅ `ios/Podfile` - معد مع تحسينات الحجم
- ✅ `ios/Runner.xcodeproj` - معد مع تحسينات الحجم
- ✅ `ios/Runner/Info.plist` - معد مع اسم التطبيق
- ✅ `.github/workflows/build_ios.yml` - سكريبت البناء التلقائي

---

## 🚀 طريقة البناء (على Windows)

بما أنك على Windows، لا يمكنك بناء iOS محلياً. الحلول المتاحة:

### الطريقة 1: GitHub Actions (الأسهل والأسرع) ⭐

1. **ادفع الكود إلى GitHub:**
   ```bash
   git add .
   git commit -m "Prepare iOS build"
   git push origin main
   ```

2. **افتح GitHub في المتصفح:**
   - اذهب إلى: `https://github.com/[username]/[repository]`
   - اضغط على تبويب **Actions**

3. **شغل البناء يدوياً:**
   - اضغط على **Build iOS** من القائمة الجانبية
   - اضغط على **Run workflow** (أزرار على اليمين)
   - اختر الفرع (main) واضغط **Run workflow**

4. **انتظر البناء:**
   - سيستغرق 5-10 دقائق
   - يمكنك متابعة التقدم في صفحة Actions

5. **حمّل الملف:**
   - بعد اكتمال البناء، اضغط على **ios-app** artifact
   - حمّل ملف `.ipa`

---

### الطريقة 2: استخدام Mac (إذا كان متاحاً)

إذا كان لديك Mac أو Mac في السحابة:

```bash
# 1. انتقل إلى مجلد المشروع
cd /path/to/project

# 2. احصل على التبعيات
flutter pub get

# 3. انتقل إلى مجلد iOS
cd ios

# 4. ثبت CocoaPods
pod install

# 5. ارجع إلى المجلد الرئيسي
cd ..

# 6. بناء IPA
flutter build ipa --release
```

الملف سيكون في: `build/ios/ipa/*.ipa`

---

### الطريقة 3: استخدام Codemagic (بديل)

1. سجّل في [Codemagic](https://codemagic.io)
2. اربط مستودع GitHub
3. اختر iOS build
4. شغّل البناء

---

## 📱 تثبيت IPA على iPhone

### للاختبار (TestFlight أو Development):

1. **استخدم Xcode:**
   - افتح Xcode
   - Window → Devices and Simulators
   - اسحب ملف `.ipa` إلى الجهاز

2. **أو استخدم Apple Configurator 2**

### للنشر (App Store):

1. استخدم **Transporter** أو **Xcode**
2. ارفع إلى App Store Connect
3. أرسل للمراجعة

---

## ⚙️ إعدادات البناء المطبقة

- ✅ تحسينات الحجم (Size Optimizations)
- ✅ Swift Optimization Level: `-Osize`
- ✅ GCC Optimization Level: `s`
- ✅ Bitcode: Disabled
- ✅ iOS Deployment Target: 12.0

---

## 🔧 استكشاف الأخطاء

### خطأ: "No such module"
```bash
cd ios
pod install
cd ..
flutter clean
flutter pub get
```

### خطأ: "Code signing"
- تأكد من وجود Apple Developer Account
- أضف شهادة التوقيع في Xcode

### خطأ: "Provisioning profile"
- أنشئ Provisioning Profile في Apple Developer Portal
- أضفه في Xcode

---

## 📞 ملاحظات

- البناء على GitHub Actions مجاني للاستخدام العام
- كل بناء يستغرق حوالي 5-10 دقائق
- الملفات المحفوظة لمدة 30 يوم

---

## 🎯 الأمر السريع (على Mac فقط)

```bash
flutter build ipa --release
```

الملف: `build/ios/ipa/*.ipa`





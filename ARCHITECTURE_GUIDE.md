# دليل البنية المعمارية - التطبيق المحدث

## 📁 هيكل المشروع الجديد

```
lib/
├── core/                          # الكود الأساسي المشترك
│   ├── models/                    # نماذج البيانات
│   │   ├── account_model.dart
│   │   ├── currency_model.dart
│   │   └── user_profile_model.dart
│   └── services/                  # خدمات الأعمال
│       ├── account_service.dart
│       ├── currency_service.dart
│       └── user_profile_service.dart
│
├── features/                       # الميزات منفصلة
│   ├── accounts/                   # ميزة الحسابات
│   │   └── screens/
│   │       ├── accounts_list_screen.dart
│   │       └── add_edit_account_screen.dart
│   │
│   ├── currencies/                # ميزة العملات
│   │   └── screens/
│   │       ├── currencies_list_screen.dart
│   │       └── add_edit_currency_screen.dart
│   │
│   └── profile/                   # ميزة الملف الشخصي
│       └── screens/
│           └── user_profile_screen.dart
│
├── shared/                         # المكونات المشتركة
│   ├── theme/
│   │   └── app_theme.dart         # نظام التصميم الموحد
│   └── widgets/
│       ├── app_card.dart          # بطاقة موحدة
│       ├── loading_overlay.dart   # شاشة التحميل
│       └── confirm_dialog.dart    # حوار التأكيد
│
└── [الشاشات القديمة]              # الشاشات القديمة (للتوافق)
```

---

## 🎨 نظام التصميم الموحد

### الألوان (الهوية البصرية)
- **اللون الأساسي**: Maroon (#800000)
- **اللون الثانوي**: Gold (#E8B81C)
- **النجاح**: أخضر
- **الخطأ**: أحمر

### المكونات الموحدة

#### AppCard
بطاقة موحدة مع تصميم احترافي:
```dart
AppCard(
  child: Text('المحتوى'),
  elevated: true, // للظلال الأقوى
)
```

#### LoadingOverlay
شاشة تحميل موحدة:
```dart
LoadingOverlay(
  isLoading: _isLoading,
  child: Widget(),
)
```

#### ConfirmDialog
حوار تأكيد موحد:
```dart
final confirmed = await showConfirmDialog(
  context: context,
  title: 'العنوان',
  message: 'الرسالة',
  isDanger: true,
);
```

---

## 🔧 الخدمات (Services)

### AccountService
إدارة الحسابات المالية:
- `streamAccounts()` - جلب جميع الحسابات
- `createAccount()` - إنشاء حساب جديد
- `updateAccount()` - تحديث حساب
- `deleteAccount()` - حذف حساب (مع التحقق من الاستخدام)
- `isAccountUsed()` - التحقق من استخدام الحساب

### CurrencyService
إدارة العملات:
- `streamCurrencies()` - جلب جميع العملات
- `createCurrency()` - إنشاء عملة جديدة
- `updateCurrency()` - تحديث عملة
- `deleteCurrency()` - حذف عملة (مع التحقق من الاستخدام)
- `isCurrencyUsed()` - التحقق من استخدام العملة
- `getBaseCurrency()` - جلب العملة الأساسية

### UserProfileService
إدارة الملف الشخصي:
- `streamUserProfile()` - جلب الملف الشخصي
- `saveUserProfile()` - حفظ/تحديث الملف الشخصي
- `updateProfileImage()` - تحديث صورة الملف الشخصي
- `updateLogo()` - تحديث الشعار

---

## 📱 الشاشات الجديدة

### 1. إدارة الحسابات
**الملف**: `lib/features/accounts/screens/accounts_list_screen.dart`

**الميزات**:
- ✅ عرض جميع الحسابات مع الأرصدة
- ✅ إضافة حساب جديد
- ✅ تعديل حساب موجود
- ✅ حذف حساب (مع منع الحذف عند الاستخدام)
- ✅ تصميم احترافي موحد

### 2. إدارة العملات
**الملف**: `lib/features/currencies/screens/currencies_list_screen.dart`

**الميزات**:
- ✅ عرض جميع العملات
- ✅ إضافة عملة جديدة
- ✅ تعديل عملة موجودة
- ✅ حذف عملة (مع منع الحذف عند الاستخدام)
- ✅ تحديد العملة الأساسية
- ✅ تصميم احترافي موحد

### 3. الملف الشخصي
**الملف**: `lib/features/profile/screens/user_profile_screen.dart`

**الميزات**:
- ✅ الاسم الكامل
- ✅ رقم الهاتف
- ✅ البريد الإلكتروني
- ✅ العنوان
- ✅ صورة الملف الشخصي (رفع من المعرض)
- ✅ الشعار (رفع من المعرض)
- ✅ تصميم احترافي موحد

---

## 🔒 الأمان والتحقق

### منع الحذف عند الاستخدام

#### للحسابات:
```dart
// التحقق من استخدام الحساب في المعاملات
final isUsed = await accountService.isAccountUsed(accountId);
if (isUsed) {
  throw Exception('لا يمكن حذف الحساب لأنه مستخدم في معاملات مالية');
}
```

#### للعملات:
```dart
// التحقق من استخدام العملة في المعاملات
final isUsed = await currencyService.isCurrencyUsed(currencyId);
if (isUsed) {
  throw Exception('لا يمكن حذف العملة لأنها مستخدمة في معاملات مالية');
}

// منع حذف العملة الأساسية
if (currency.isBase) {
  throw Exception('لا يمكن حذف العملة الأساسية');
}
```

---

## 📦 الحزم المضافة

```yaml
dependencies:
  image_picker: ^1.0.7      # لرفع الصور
  firebase_storage: ^12.0.1  # لتخزين الصور
```

---

## 🚀 كيفية الاستخدام

### إضافة حساب جديد
```dart
Navigator.of(context).push(
  MaterialPageRoute(
    builder: (_) => const AddEditAccountScreen(),
  ),
);
```

### تعديل حساب موجود
```dart
Navigator.of(context).push(
  MaterialPageRoute(
    builder: (_) => AddEditAccountScreen(account: account),
  ),
);
```

### حذف حساب
```dart
final confirmed = await showConfirmDialog(
  context: context,
  title: 'حذف الحساب',
  message: 'هل أنت متأكد؟',
  isDanger: true,
);

if (confirmed) {
  await accountService.deleteAccount(accountId);
}
```

---

## 📝 ملاحظات مهمة

1. **التوافق**: الشاشات القديمة ما زالت تعمل ولكنها تستدعي الشاشات الجديدة
2. **البيانات**: جميع البيانات محفوظة في Firebase
3. **الأمان**: كل مستخدم يرى فقط بياناته
4. **التصميم**: جميع الشاشات تستخدم نفس نظام التصميم الموحد

---

## 🔄 التحديثات المستقبلية

- [ ] إضافة State Management (Riverpod)
- [ ] تحسين الأداء
- [ ] إضافة المزيد من الميزات
- [ ] تحسين التصميم

---

**تاريخ الإنشاء**: $(date)
**الحالة**: جاهز للاستخدام ✅

